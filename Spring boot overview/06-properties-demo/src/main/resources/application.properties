# Use Wildcard "*" to expose all endpoints
# Can also expose individual endpoints with a comma-delimited list
management.endpoints.web.exposure.include=*
management.info.env.enabled=true

# Exclude individual endpoints with a comma-delimited list
# management.endpoints.web.exposure.exclude=health,info

info.app.name=My Spring Boot App
info.app.description=A new Spring boot 3 App
info.app.version=1.0.0

#
# Define my properties
#
coach.name =Spring Programmer
team.name = Programmers Club

#
# Change Spring Boot embedded server port
#
server.port=7070

#
# Set the context path of the application
#
# All request should be prefixed with /spring-boot
#
server.servlet.context-path=/spring-boot